function [TarFileName,TarFileNum] = TargetFile(TarPath)
TarFilePath = dir([TarPath,'*','.txt']);
TarFileNum = 0;
for i = 1 : length(TarFilePath)
    TarFileNum = TraFileNum +1;
end
TarFileName = cell(TarFileNum,1)